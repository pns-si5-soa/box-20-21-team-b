#apiVersion: v1
#kind: Service
#metadata:
#  labels:
#    app: kafka
#  name: kafka
#spec:
#  ports:
#  - port: 9092
#    protocol: TCP
#    targetPort: 9092
#  selector:
#    app: kafka
#  type: NodePort # Uncomment if doesnt match IP

apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  labels:
    name: boxb-kafka
spec:
  ports:
  - port: 9092
    name: kafka-port
    protocol: TCP
  selector:
    app: boxb-kafka
    id: "0"
  type: LoadBalancer
